# URL base da API.
# Deixe vazio para usar URLs relativas via proxy do Vite (padrão no Docker).
# Para acesso pelo celular na rede local, use o IP da sua máquina:
# VITE_API_URL=http://192.168.0.6:3000
VITE_API_URL=
